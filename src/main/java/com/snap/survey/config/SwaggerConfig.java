package com.snap.survey.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {

  //  @Bean
  //  public OpenAPI customOpenAPI() {
  //    return new OpenAPI()
  //        .info(
  //            new Info()
  //                .title("survey service")
  //                .version("apiVersion")
  //                .description("survey service project"))
  //        .addSecurityItem(new SecurityRequirement())
  //        .components(
  //            new Components()
  //                .addSecuritySchemes(
  //                    "",
  //                    new SecurityScheme()
  //                        .name("JWT Token Auth")
  //                        .type(SecurityScheme.Type.HTTP)
  //                        .scheme("Bearer")
  //                        .bearerFormat("JWT")));
  //  }
}
